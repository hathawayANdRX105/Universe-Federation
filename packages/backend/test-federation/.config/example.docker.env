NODE_VERSION=22
NODE_EXTRA_CA_CERTS=/usr/local/share/ca-certificates/rootCA.crt
POSTGRES_DB=misskey
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
MK_VERBOSE=true
# Instant local→remote follow (skip pending request unless target is locked)
FORCE_FOLLOW_REMOTE_USER_FOR_TESTING=true
